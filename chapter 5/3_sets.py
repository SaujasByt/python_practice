'''sets are unordered
sets cannot contain list
sets are unindexed(you cannot find elements using index)
items in a set cannot be changed
sets cannot contIn duplicate items'''

a={} #this will create a empty dictionary
print(type(a))

b=set() #this will create a empty set
print(type(b))
